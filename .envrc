# Load Nix development environment for emailai project
# Use nix develop with flake support

# Check if we're in a nix environment, if not, show helpful message
if [ -z "$IN_NIX_SHELL" ]; then
    echo "💡 To activate the Nix environment, run: nix develop --accept-flake-config"
fi

# Add any project-specific environment variables here
export PROJECT_NAME="emailai"
export GOPATH="$HOME/Development/go" # Put your Path here

# Go cache configuration (separate from workspace)
export GOMODCACHE="$HOME/.cache/go/mod"
export GOCACHE="$HOME/.cache/go/build"
# Note: GOSUMDB is already set by flake.nix env; avoid duplicate definitions
# export GOSUMDB="sum.golang.org"
